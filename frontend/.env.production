# 生产环境（前端规格 §54 / §56）
#
# 生产由 Nginx 提供两个入口：`/` → Vue 静态文件，`/api/` → FastAPI。
# 因此基地址是相对路径，与开发环境一致。
#
# ⚠️ 严禁在此文件写入任何密钥（数据库密码 / JWT Secret / Redis 密码）：
#    VITE_* 会被内联进 JS 产物，任何访问者都能在源码里看到。
VITE_APP_TITLE=团队任务协作系统
VITE_API_BASE_URL=/api/v1
