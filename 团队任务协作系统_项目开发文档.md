# 团队任务协作系统后端

> 项目代号：TaskFlow Pro  
> 项目定位：生产级团队任务协作平台后端  
> 开发方式：Cursor + AI 辅助开发  
> 核心原则：真实实现、分层清晰、可测试、可部署、可解释，禁止为了“看起来高级”而堆砌无实际用途的技术。

---

## 1. 项目目标

将原有团队任务管理系统重构为一个具备生产级后端工程能力的团队任务协作平台。

系统主要解决：

- 用户注册、登录及身份认证
- 基于 RBAC 的角色与权限控制
- 团队、成员、项目管理
- 任务创建、分配及状态流转
- 多人任务分配
- 评论与附件
- 操作审计日志
- 站内通知
- Redis 限流
- Celery 异步任务
- PostgreSQL 数据建模与索引优化
- API 自动化测试
- Docker 部署
- CI 持续集成
- 基础健康检查、日志与指标

最终目标不是简单完成 CRUD，而是形成一个可以用于 Python 后端求职、面试讲解和 GitHub 展示的完整项目。

---

# 2. 技术栈

## 2.1 后端

- Python 3.11 / 3.12
- FastAPI
- Pydantic v2
- SQLAlchemy 2.0
- Alembic

## 2.2 数据库

- PostgreSQL 16
- Redis 7

## 2.3 异步任务

- Celery
- Redis 作为 Broker / Backend

## 2.4 认证

- JWT
- Access Token
- Refresh Token
- Refresh Token JTI
- Token 黑名单

## 2.5 测试

- pytest
- pytest-asyncio
- httpx

## 2.6 部署

- Docker
- Docker Compose
- Nginx
- Uvicorn / Gunicorn

## 2.7 CI

- GitHub Actions

---

# 3. 开发约束

Cursor 开发过程中必须遵守以下规则。

## 3.1 代码必须真实可运行

禁止：

- 伪代码冒充实现
- 创建没有实际用途的空模块
- 接口返回固定假数据
- 虚构数据库查询结果
- 虚构第三方服务
- 为了简历包装而添加无法解释的技术

所有代码都必须能够运行、测试和解释。

---

## 3.2 严格分层

项目必须遵循：

```text
Router
   ↓
Service
   ↓
CRUD
   ↓
Model
```

职责：

### Router

负责：

- 接收 HTTP 请求
- 参数校验
- 调用 Service
- 返回响应
- HTTP 状态码处理

Router 不负责：

- 复杂业务逻辑
- 直接编写数据库查询
- 状态机判断
- 权限核心判断

---

### Service

负责：

- 核心业务逻辑
- 权限业务判断
- 状态机
- 事务协调
- 多个 CRUD 操作组合
- Redis / Celery 等基础设施调用协调

---

### CRUD

负责：

- 数据库增删改查
- 查询条件封装
- 分页
- 数据持久化

CRUD 不负责：

- HTTP
- 用户业务权限
- 状态机规则

---

### Model

负责：

- SQLAlchemy ORM 模型
- 字段定义
- 表关系
- 数据库约束映射

---

# 4. 项目目录

最终项目结构：

```text
task-flow/
├── app/
│   ├── main.py
│   │
│   ├── core/
│   │   ├── config.py
│   │   ├── security.py
│   │   ├── deps.py
│   │   └── exceptions.py
│   │
│   ├── db/
│   │   ├── session.py
│   │   └── base.py
│   │
│   ├── models/
│   │   ├── user.py
│   │   ├── role.py
│   │   ├── permission.py
│   │   ├── team.py
│   │   ├── team_member.py
│   │   ├── project.py
│   │   ├── task.py
│   │   ├── task_assignee.py
│   │   ├── comment.py
│   │   ├── attachment.py
│   │   ├── operation_log.py
│   │   ├── notification.py
│   │   └── refresh_token.py
│   │
│   ├── schemas/
│   │   ├── auth.py
│   │   ├── user.py
│   │   ├── team.py
│   │   ├── project.py
│   │   ├── task.py
│   │   ├── comment.py
│   │   ├── attachment.py
│   │   ├── notification.py
│   │   └── common.py
│   │
│   ├── crud/
│   │   ├── user.py
│   │   ├── role.py
│   │   ├── team.py
│   │   ├── project.py
│   │   ├── task.py
│   │   ├── comment.py
│   │   ├── attachment.py
│   │   ├── operation_log.py
│   │   ├── notification.py
│   │   └── refresh_token.py
│   │
│   ├── services/
│   │   ├── auth_service.py
│   │   ├── user_service.py
│   │   ├── team_service.py
│   │   ├── project_service.py
│   │   ├── task_service.py
│   │   ├── comment_service.py
│   │   ├── attachment_service.py
│   │   ├── notification_service.py
│   │   └── operation_log_service.py
│   │
│   ├── api/
│   │   └── v1/
│   │       ├── router.py
│   │       ├── auth.py
│   │       ├── users.py
│   │       ├── teams.py
│   │       ├── projects.py
│   │       ├── tasks.py
│   │       ├── comments.py
│   │       ├── attachments.py
│   │       ├── notifications.py
│   │       └── logs.py
│   │
│   ├── tasks/
│   │   ├── celery_app.py
│   │   └── notification_tasks.py
│   │
│   ├── middleware/
│   │   └── rate_limit.py
│   │
│   └── utils/
│       ├── enums.py
│       └── pagination.py
│
├── tests/
│   ├── conftest.py
│   ├── test_auth.py
│   ├── test_users.py
│   ├── test_teams.py
│   ├── test_projects.py
│   ├── test_tasks.py
│   ├── test_comments.py
│   ├── test_notifications.py
│   └── test_rate_limit.py
│
├── alembic/
│   ├── versions/
│   └── env.py
│
├── .github/
│   └── workflows/
│       └── ci.yml
│
├── nginx/
│   └── nginx.conf
│
├── Dockerfile
├── docker-compose.yml
├── docker-compose.prod.yml
├── alembic.ini
├── requirements.txt
├── .env.example
├── .gitignore
└── README.md
```

---

# 5. 核心业务模型

## 5.1 用户

users：

```text
id
username
email
password_hash
is_active
created_at
updated_at
```

要求：

- username 唯一
- email 唯一
- 密码不能明文保存
- API 返回禁止泄露 password_hash

---

# 6. RBAC 权限系统

核心表：

```text
users
roles
permissions
user_roles
role_permissions
```

权限模型：

```text
User
  ↓
UserRole
  ↓
Role
  ↓
RolePermission
  ↓
Permission
```

权限示例：

```text
user:read
user:update

team:create
team:read
team:update
team:delete
team:invite

project:create
project:read
project:update
project:delete

task:create
task:read
task:update
task:delete
task:assign
task:transition

comment:create
comment:delete

attachment:upload
attachment:download

log:read
```

Service 层必须完成业务权限校验。

---

# 7. 团队模型

teams：

```text
id
name
description
owner_id
created_at
updated_at
```

team_members：

```text
id
team_id
user_id
role_id
joined_at
```

约束：

```text
一个用户不能重复加入同一个团队
```

团队角色示例：

```text
OWNER
ADMIN
MEMBER
```

---

# 8. 项目模型

projects：

```text
id
team_id
name
description
created_by
created_at
updated_at
```

规则：

- 项目必须属于一个团队
- 创建项目的用户必须属于该团队
- 项目成员权限继承团队关系
- 项目不能跨团队关联任务

---

# 9. 任务模型

tasks：

```text
id
project_id
title
description
status
priority
creator_id
due_at
created_at
updated_at
```

任务状态：

```text
TODO
IN_PROGRESS
REVIEW
DONE
CANCELLED
```

优先级：

```text
LOW
MEDIUM
HIGH
URGENT
```

---

# 10. 多人任务分配

使用：

```text
task_assignees
```

字段：

```text
id
task_id
user_id
assigned_by
created_at
```

关系：

```text
Task
  ↓
TaskAssignee
  ↓
User
```

任务可以分配给多个用户。

必须防止：

```text
同一个任务重复分配给同一个用户
```

数据库增加唯一约束：

```text
UNIQUE(task_id, user_id)
```

---

# 11. 任务状态机

允许：

```text
TODO -> IN_PROGRESS
IN_PROGRESS -> REVIEW
REVIEW -> DONE

任意状态 -> CANCELLED
```

禁止：

```text
DONE -> TODO
DONE -> IN_PROGRESS
DONE -> REVIEW
```

非法状态流转必须在 Service 层拦截。

推荐实现：

```python
ALLOWED_TRANSITIONS = {
    "TODO": {"IN_PROGRESS", "CANCELLED"},
    "IN_PROGRESS": {"REVIEW", "CANCELLED"},
    "REVIEW": {"DONE", "CANCELLED"},
    "DONE": set(),
    "CANCELLED": set(),
}
```

状态流转必须通过专用 Service：

```text
POST /api/v1/tasks/{task_id}/transition
```

不要允许普通 PATCH 任意修改 status。

---

# 12. PostgreSQL 设计

数据库：

```text
PostgreSQL 16
```

使用：

```text
TIMESTAMPTZ
JSONB
GIN
pg_trgm
tsvector
```

---

# 13. tasks 表核心设计

参考：

```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;

CREATE TABLE tasks (
    id BIGSERIAL PRIMARY KEY,
    project_id BIGINT NOT NULL REFERENCES projects(id),
    title TEXT NOT NULL,
    description TEXT,
    status VARCHAR(20) NOT NULL DEFAULT 'TODO'
        CHECK (
            status IN (
                'TODO',
                'IN_PROGRESS',
                'REVIEW',
                'DONE',
                'CANCELLED'
            )
        ),
    priority VARCHAR(10) NOT NULL DEFAULT 'MEDIUM'
        CHECK (
            priority IN (
                'LOW',
                'MEDIUM',
                'HIGH',
                'URGENT'
            )
        ),
    creator_id BIGINT NOT NULL REFERENCES users(id),
    due_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

索引：

```sql
CREATE INDEX idx_tasks_project_status
ON tasks(project_id, status);

CREATE INDEX idx_tasks_creator
ON tasks(creator_id);

CREATE INDEX idx_tasks_due_at
ON tasks(due_at)
WHERE status NOT IN ('DONE', 'CANCELLED');
```

---

# 14. 全文搜索

启用：

```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;
```

标题模糊搜索：

```sql
CREATE INDEX idx_tasks_title_trgm
ON tasks USING GIN (title gin_trgm_ops);
```

全文搜索：

```sql
ALTER TABLE tasks
ADD COLUMN search_vector tsvector
GENERATED ALWAYS AS (
    to_tsvector(
        'simple',
        coalesce(title, '') || ' ' || coalesce(description, '')
    )
) STORED;

CREATE INDEX idx_tasks_search
ON tasks USING GIN(search_vector);
```

目标：

支持任务：

```text
关键词搜索
状态过滤
优先级过滤
项目过滤
负责人过滤
截止时间过滤
分页
排序
```

---

# 15. 操作日志

operation_logs：

```text
id
user_id
resource_type
resource_id
action
payload
created_at
```

SQL：

```sql
CREATE TABLE operation_logs (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL,
    resource_type VARCHAR(50) NOT NULL,
    resource_id BIGINT NOT NULL,
    action VARCHAR(50) NOT NULL,
    payload JSONB NOT NULL DEFAULT '{}'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

索引：

```sql
CREATE INDEX idx_operation_logs_payload
ON operation_logs
USING GIN(payload jsonb_path_ops);

CREATE INDEX idx_operation_logs_resource
ON operation_logs(resource_type, resource_id);

CREATE INDEX idx_operation_logs_user_created
ON operation_logs(user_id, created_at DESC);
```

记录示例：

```json
{
  "old_status": "TODO",
  "new_status": "IN_PROGRESS"
}
```

日志必须记录：

```text
谁
什么时候
对什么资源
执行了什么操作
操作前后的关键数据
```

---

# 16. 评论

comments：

```text
id
task_id
user_id
content
created_at
updated_at
```

规则：

- 用户必须有任务访问权限才能评论
- 评论必须属于任务
- 删除评论需要权限
- 删除行为写入操作日志

---

# 17. 附件

attachments：

```text
id
task_id
uploader_id
filename
storage_path
content_type
size
created_at
```

当前阶段可以使用本地文件存储：

```text
storage/
```

设计时必须预留未来迁移对象存储的可能性。

上传要求：

- 文件大小限制
- MIME 类型校验
- 文件名安全处理
- 不允许路径穿越
- 下载时检查任务访问权限

---

# 18. 通知系统

notifications：

```text
id
user_id
type
title
content
is_read
created_at
```

通知场景：

```text
任务分配
任务状态变更
任务被评论
团队邀请
```

接口：

```text
GET /api/v1/notifications
PATCH /api/v1/notifications/{id}/read
```

---

# 19. JWT 双 Token

使用：

```text
Access Token
Refresh Token
```

Access Token：

```text
短生命周期
```

Refresh Token：

```text
较长生命周期
```

Refresh Token 数据库存储：

```text
refresh_tokens
```

字段：

```text
id
user_id
jti
expires_at
revoked
created_at
```

Refresh 流程：

```text
Login
 ↓
Access Token + Refresh Token
 ↓
Access Token 过期
 ↓
Refresh
 ↓
验证 Refresh Token
 ↓
检查数据库 JTI
 ↓
生成新的 Token
```

登出：

```text
撤销 Refresh Token
```

必要时支持 Redis JWT 黑名单。

---

# 20. 密码安全

禁止：

```text
明文保存密码
```

推荐：

```text
pwdlib / Argon2
```

密码验证必须位于：

```text
core/security.py
```

---

# 21. Redis

Redis 用途：

1. JWT 黑名单
2. API 限流
3. Celery Broker
4. Celery Backend
5. 后续可扩展缓存

---

# 22. Redis 滑动窗口限流

实现：

```text
Redis ZSET + Lua
```

目标：

```text
IP / User
 ↓
Redis ZSET
 ↓
滑动时间窗口
 ↓
判断请求数量
 ↓
超过限制返回 429
```

Lua 脚本需要保证：

```text
删除窗口外数据
统计当前窗口请求数
判断是否超限
新增当前请求
设置过期时间
```

必须保证 Redis 操作原子性。

---

# 23. Celery

Celery 用于处理不应该阻塞 HTTP 请求的任务。

示例：

```text
发送任务通知
发送团队邀请
归档操作日志
清理过期附件
```

架构：

```text
FastAPI
   ↓
Celery
   ↓
Redis Broker
   ↓
Celery Worker
   ↓
执行任务
```

HTTP 请求不应该等待耗时任务完成。

---

# 24. 通知异步化

例如：

```text
用户 A 分配任务给用户 B
        ↓
TaskService
        ↓
创建任务
        ↓
提交 Celery Task
        ↓
HTTP 快速返回
        ↓
Worker
        ↓
创建通知
```

需要保证：

- 主业务失败不能产生错误通知
- Celery 任务失败可重试
- 重试不会产生大量重复通知

---

# 25. API 设计

基础路径：

```text
/api/v1
```

---

## 25.1 Auth

```text
POST /api/v1/auth/register
POST /api/v1/auth/login
POST /api/v1/auth/refresh
POST /api/v1/auth/logout
```

---

## 25.2 User

```text
GET /api/v1/users/me
```

---

## 25.3 Team

```text
POST /api/v1/teams
GET /api/v1/teams
GET /api/v1/teams/{team_id}
PATCH /api/v1/teams/{team_id}
DELETE /api/v1/teams/{team_id}

POST /api/v1/teams/{team_id}/members
GET /api/v1/teams/{team_id}/members
DELETE /api/v1/teams/{team_id}/members/{user_id}
```

---

## 25.4 Project

```text
POST /api/v1/projects
GET /api/v1/projects
GET /api/v1/projects/{project_id}
PATCH /api/v1/projects/{project_id}
DELETE /api/v1/projects/{project_id}
```

---

## 25.5 Task

```text
POST /api/v1/tasks

GET /api/v1/tasks

GET /api/v1/tasks/{task_id}

PATCH /api/v1/tasks/{task_id}

DELETE /api/v1/tasks/{task_id}

POST /api/v1/tasks/{task_id}/transition

POST /api/v1/tasks/{task_id}/assignees

DELETE /api/v1/tasks/{task_id}/assignees/{user_id}
```

任务列表支持：

```text
project_id
status
priority
assignee_id
keyword
due_at
page
page_size
```

---

## 25.6 Comment

```text
POST /api/v1/tasks/{task_id}/comments
GET /api/v1/tasks/{task_id}/comments
DELETE /api/v1/comments/{comment_id}
```

---

## 25.7 Attachment

```text
POST /api/v1/tasks/{task_id}/attachments
GET /api/v1/attachments/{attachment_id}
DELETE /api/v1/attachments/{attachment_id}
```

---

## 25.8 Notification

```text
GET /api/v1/notifications
PATCH /api/v1/notifications/{notification_id}/read
PATCH /api/v1/notifications/read-all
```

---

## 25.9 Logs

```text
GET /api/v1/logs
GET /api/v1/logs/{resource_type}/{resource_id}
```

只有具备日志查看权限的角色可以访问。

---

# 26. API 响应规范

成功响应：

```json
{
  "data": {},
  "message": "success"
}
```

分页响应：

```json
{
  "data": [],
  "page": 1,
  "page_size": 20,
  "total": 100
}
```

错误响应：

```json
{
  "detail": "Task status transition is not allowed"
}
```

HTTP 状态码：

```text
200 OK
201 Created
204 No Content
400 Bad Request
401 Unauthorized
403 Forbidden
404 Not Found
409 Conflict
422 Unprocessable Entity
429 Too Many Requests
500 Internal Server Error
```

---

# 27. 数据库事务原则

涉及多个数据库操作时必须使用事务。

例如：

```text
创建任务
 ↓
写入任务
 ↓
写入任务分配
 ↓
写入操作日志
 ↓
提交事务
```

任意步骤失败：

```text
全部回滚
```

Service 负责事务协调。

---

# 28. Alembic

所有数据库结构变化必须通过 Alembic。

禁止：

```text
直接手动修改生产数据库
```

开发流程：

```bash
alembic revision --autogenerate -m "create users"
alembic upgrade head
```

回滚：

```bash
alembic downgrade -1
```

---

# 29. Docker Compose

开发环境至少包含：

```text
postgres
redis
api
celery_worker
```

可选：

```text
nginx
```

服务关系：

```text
nginx
  ↓
api
  ↓
postgres
redis
  ↓
celery_worker
```

---

# 30. Dockerfile

要求：

- 使用 Python 官方基础镜像
- 非 root 用户运行
- 使用 requirements.txt
- 设置 PYTHONDONTWRITEBYTECODE
- 设置 PYTHONUNBUFFERED
- 健康检查
- 合理的启动命令

---

# 31. Nginx

生产环境：

```text
Client
  ↓
Nginx
  ↓
Gunicorn
  ↓
Uvicorn Worker
  ↓
FastAPI
```

Nginx 负责：

- 反向代理
- 请求体大小限制
- 基础超时配置
- 静态附件访问
- 基础安全 Header

---

# 32. 健康检查

提供：

```text
GET /health
GET /health/db
GET /health/redis
```

例如：

```json
{
  "status": "ok"
}
```

数据库不可用：

```json
{
  "status": "error",
  "database": "unavailable"
}
```

---

# 33. 日志

使用 Python logging。

生产环境采用结构化日志格式。

至少记录：

```text
timestamp
level
logger
message
request_id
user_id
path
method
status_code
duration
```

禁止日志输出：

```text
password
access_token
refresh_token
完整敏感信息
```

---

# 34. Request ID

每一个 HTTP 请求生成：

```text
request_id
```

可以由客户端传入，也可以服务端生成。

日志中必须带：

```text
request_id
```

方便排查问题。

---

# 35. pytest

测试必须覆盖核心业务，而不是只测试接口能返回 200。

重点测试：

## Auth

```text
注册成功
重复用户名
重复邮箱
错误密码
登录成功
Refresh 成功
Refresh Token 撤销
Logout
```

## RBAC

```text
有权限访问
无权限访问
不同角色权限差异
```

## Task

```text
创建任务
查询任务
修改任务
删除任务
任务分配
多人分配
重复分配
状态正常流转
非法状态流转
DONE 不允许回退
```

## Team

```text
创建团队
邀请成员
重复成员
非管理员不能邀请
```

## Rate Limit

```text
正常请求
达到限制
超过限制返回 429
窗口恢复后允许请求
```

---

# 36. 测试数据库

测试环境不要污染开发数据库。

推荐：

```text
pytest
 ↓
独立测试数据库
 ↓
每个测试清理数据
```

可以通过 fixture 管理：

```text
db_session
client
test_user
test_team
test_project
test_task
```

---

# 37. GitHub Actions

CI 至少执行：

```text
安装依赖
 ↓
代码检查
 ↓
pytest
 ↓
构建 Docker 镜像
```

推荐：

```text
ruff
pytest
docker build
```

如果条件允许增加：

```text
Alembic migration test
```

---

# 38. 项目开发阶段

必须严格按照阶段执行。

---

## Phase 1：项目骨架

完成：

- Git 初始化
- FastAPI
- 配置系统
- PostgreSQL
- Redis
- Docker Compose
- SQLAlchemy
- Alembic
- 基础目录
- `/health`

验收：

```text
docker compose up -d
```

API 可以正常启动。

---

## Phase 2：用户系统

完成：

- User Model
- User Schema
- User CRUD
- 注册
- 密码哈希
- 登录
- JWT
- `/users/me`

验收：

```text
注册
 ↓
登录
 ↓
获取 Access Token
 ↓
访问 /users/me
```

---

## Phase 3：Refresh Token

完成：

- refresh_tokens
- Refresh Token
- JTI
- Refresh
- Logout
- Token revoke

验收：

```text
Access Token
Refresh Token
Logout
Refresh 失败
```

---

## Phase 4：RBAC

完成：

- Role
- Permission
- UserRole
- RolePermission
- 权限依赖
- Service 权限检查

验收：

```text
ADMIN
MEMBER
```

权限表现不同。

---

## Phase 5：团队

完成：

- Team
- TeamMember
- 创建团队
- 邀请成员
- 删除成员
- 团队权限

---

## Phase 6：项目

完成：

- Project
- 项目 CRUD
- 项目与团队关系
- 项目访问权限

---

## Phase 7：任务

完成：

- Task
- TaskAssignee
- CRUD
- 多人分配
- 查询过滤
- 分页
- 排序

---

## Phase 8：状态机

完成：

- 状态枚举
- transition API
- Service 状态机
- 非法状态拦截
- 操作日志

重点演示：

```text
TODO
 ↓
IN_PROGRESS
 ↓
REVIEW
 ↓
DONE
```

以及：

```text
DONE -> TODO
```

被拒绝。

---

## Phase 9：评论与附件

完成：

- Comment
- Attachment
- 文件上传
- 文件下载
- 权限检查
- 文件大小限制

---

## Phase 10：操作日志

完成：

- operation_logs
- 日志 Service
- 任务操作日志
- 团队操作日志
- 权限日志查询

---

## Phase 11：Redis 限流

完成：

- Redis connection
- ZSET
- Lua
- Sliding Window
- 429
- 测试

---

## Phase 12：Celery

完成：

- Celery App
- Redis Broker
- Worker
- 通知任务
- 日志归档任务
- 附件清理任务

---

## Phase 13：通知

完成：

- Notification Model
- 站内信
- 任务分配通知
- 状态变更通知
- 已读 / 未读

---

## Phase 14：测试

完成：

```text
pytest
```

覆盖核心业务。

目标：

```text
核心 Service + API 有较高测试覆盖率
```

不要为了追求数字而测试没有业务价值的代码。

---

## Phase 15：部署

完成：

- Dockerfile
- Docker Compose
- Nginx
- Gunicorn
- Uvicorn Worker
- 环境变量
- 健康检查

---

## Phase 16：CI

完成：

```text
.github/workflows/ci.yml
```

实现：

```text
push
 ↓
GitHub Actions
 ↓
lint
 ↓
pytest
 ↓
docker build
```

---

## Phase 17：项目文档

README 必须包含：

```text
项目介绍
技术栈
系统架构
目录结构
ER 图
核心业务流程
API 文档
环境配置
Docker 启动
数据库迁移
测试
CI
性能优化
Redis 限流原理
Celery 原理
JWT 原理
状态机设计
项目难点
解决方案
```

---

# 39. Cursor 开发规则

Cursor 不允许一次性生成整个项目。

每次只完成一个明确功能。

推荐流程：

```text
需求确认
 ↓
设计
 ↓
Model
 ↓
Migration
 ↓
Schema
 ↓
CRUD
 ↓
Service
 ↓
Router
 ↓
Test
 ↓
运行验证
 ↓
提交 Git
```

---

# 40. Cursor 每次开发必须遵循

当开始一个新模块时：

### 第一步

先分析：

```text
需要哪些 Model
需要哪些字段
需要哪些关系
需要哪些约束
需要哪些索引
```

### 第二步

实现 Model。

### 第三步

生成 Alembic migration。

### 第四步

实现 Schema。

### 第五步

实现 CRUD。

### 第六步

实现 Service。

### 第七步

实现 Router。

### 第八步

编写 pytest。

### 第九步

运行：

```bash
pytest
```

### 第十步

确认通过后再进入下一个模块。

---

# 41. Cursor 禁止事项

禁止 Cursor：

```text
一次生成几十个文件
```

禁止：

```text
没有运行测试就说完成
```

禁止：

```text
为了修复一个 Bug 重构整个项目
```

禁止：

```text
随意改变已经确认的目录结构
```

禁止：

```text
删除已有功能但没有说明
```

禁止：

```text
偷偷修改数据库结构
```

禁止：

```text
把业务逻辑全部写进 Router
```

禁止：

```text
直接在 Router 操作 SQLAlchemy Session 完成复杂业务
```

禁止：

```text
没有需求就新增第三方依赖
```

---

# 42. AI 辅助开发原则

AI 的作用：

```text
解释
分析
提供实现方案
生成局部代码
帮助排查错误
补充测试
```

最终代码必须由开发者理解。

开发过程中每完成一个功能，需要能够回答：

```text
为什么这么设计？
为什么放在 Service？
为什么这里需要事务？
为什么需要 Redis？
为什么使用 ZSET？
为什么使用 Lua？
为什么需要 Celery？
为什么不能直接修改 status？
为什么需要 RBAC？
为什么使用 PostgreSQL GIN？
```

---

# 43. 数据库设计原则

必须优先考虑：

```text
数据一致性
查询效率
约束
索引
事务
```

不要：

```text
为了减少代码而牺牲数据完整性
```

例如：

```text
唯一关系
外键
CHECK
NOT NULL
```

能由数据库保证的规则，应尽量由数据库兜底。

业务规则仍由 Service 实现。

---

# 44. 缓存原则

Redis 不是“所有东西都放进去”。

只有存在实际收益时使用缓存。

当前主要用于：

```text
限流
Token 黑名单
Celery
```

后续如果出现明显热点查询，再考虑：

```text
缓存项目列表
缓存权限
缓存用户信息
```

---

# 45. 性能优化

重点优化：

## 数据库

```text
合理索引
避免 N+1
分页
批量查询
EXPLAIN ANALYZE
```

## Redis

```text
原子操作
Lua
合理 TTL
```

## API

```text
异步 IO
连接池
分页
避免无意义查询
```

## Celery

```text
耗时任务异步化
重试
任务幂等
```

---

# 46. N+1 查询问题

例如任务列表需要：

```text
Task
Project
Creator
Assignees
```

禁止：

```text
查询 100 个 Task
 ↓
循环 100 次查询 User
```

应该使用：

```text
selectinload
joinedload
批量查询
```

根据具体关系选择合适的加载策略。

---

# 47. 分页

统一使用：

```text
page
page_size
```

默认：

```text
page = 1
page_size = 20
```

最大：

```text
page_size <= 100
```

数据库查询必须使用：

```text
LIMIT
OFFSET
```

后续可以根据性能需要升级 Cursor Pagination。

---

# 48. 安全要求

必须考虑：

```text
SQL 注入
XSS
CSRF
JWT 泄露
密码泄露
越权访问
文件上传漏洞
路径穿越
暴力登录
接口刷请求
敏感日志泄露
```

重点：

```text
认证 ≠ 授权
```

用户拥有有效 Token，不代表用户拥有资源操作权限。

---

# 49. 资源级权限

例如：

```text
用户属于 Team A
```

不能因此访问：

```text
Team B
```

任务访问必须验证：

```text
User
 ↓
Team
 ↓
Project
 ↓
Task
```

防止 IDOR：

```text
GET /tasks/100
```

用户即使知道 task_id，也不能直接访问不属于自己的任务。

---

# 50. 统一异常处理

自定义业务异常：

```text
AuthenticationError
PermissionDeniedError
ResourceNotFoundError
ConflictError
InvalidTransitionError
RateLimitExceededError
```

统一映射 HTTP 状态码。

不要：

```python
except Exception:
    return {"message": "error"}
```

这种方式会吞掉真正的问题。

---

# 51. 配置管理

所有环境相关配置放入：

```text
.env
```

示例：

```text
APP_ENV=development

DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/taskflow

REDIS_URL=redis://redis:6379/0

JWT_SECRET_KEY=change-me

ACCESS_TOKEN_EXPIRE_MINUTES=30

REFRESH_TOKEN_EXPIRE_DAYS=7

UPLOAD_DIR=storage

MAX_UPLOAD_SIZE=10485760
```

仓库只提交：

```text
.env.example
```

不要提交真实：

```text
密码
JWT Secret
API Key
```

---

# 52. Git 提交规范

推荐：

```text
feat: add user authentication
feat: add refresh token
feat: add RBAC permission system
feat: add team management
feat: add task state machine
feat: add redis sliding window rate limit
feat: add celery notification task

fix: fix task transition validation
fix: fix permission check

test: add task service tests
docs: update architecture documentation
refactor: simplify task service
```

---

# 53. Git 分支

推荐：

```text
main
develop
feature/*
fix/*
```

例如：

```text
feature/auth
feature/rbac
feature/task
feature/rate-limit
```

---

# 54. 最终系统架构

```text
                    Client
                      │
                      ▼
                   Nginx
                      │
                      ▼
              Gunicorn + Uvicorn
                      │
                      ▼
                   FastAPI
                      │
          ┌───────────┼───────────┐
          ▼           ▼           ▼
       Router      Service      Security
                      │
                      ▼
                    CRUD
                      │
                      ▼
                PostgreSQL 16

FastAPI
   │
   ├──────────────► Redis
   │                  │
   │                  ├── Rate Limit
   │                  ├── JWT Blacklist
   │                  └── Celery Broker
   │
   └──────────────► Celery
                      │
                      ▼
                   Worker
                      │
                      ├── Notification
                      ├── Log Archive
                      └── Attachment Cleanup
```

---

# 55. 核心业务流程

## 55.1 登录

```text
Client
 ↓
POST /auth/login
 ↓
Router
 ↓
AuthService
 ↓
查询 User
 ↓
验证密码
 ↓
生成 Access Token
 ↓
生成 Refresh Token
 ↓
保存 Refresh Token JTI
 ↓
返回 Token
```

---

## 55.2 创建任务

```text
Client
 ↓
POST /tasks
 ↓
JWT Authentication
 ↓
Router
 ↓
TaskService
 ↓
验证 Project
 ↓
验证 Team Membership
 ↓
验证 Permission
 ↓
CRUD 创建 Task
 ↓
写 OperationLog
 ↓
提交事务
 ↓
Celery 通知
 ↓
返回 Task
```

---

## 55.3 状态流转

```text
Client
 ↓
POST /tasks/{id}/transition
 ↓
JWT
 ↓
Permission
 ↓
TaskService
 ↓
查询 Task
 ↓
验证当前状态
 ↓
验证目标状态
 ↓
执行状态变更
 ↓
写 OperationLog
 ↓
提交
 ↓
Celery 通知
 ↓
返回
```

---

# 56. 面试重点

项目完成后必须能够独立解释以下问题：

## FastAPI

1. FastAPI 为什么适合这个项目？
2. Depends 做什么？
3. async/await 如何工作？
4. Pydantic 做什么？

## SQLAlchemy

5. SQLAlchemy 2.0 有什么变化？
6. Session 生命周期怎么管理？
7. ORM 和 SQL 有什么关系？
8. 如何解决 N+1？

## PostgreSQL

9. 为什么选择 PostgreSQL？
10. GIN 索引是什么？
11. JSONB 有什么优势？
12. pg_trgm 用来做什么？
13. 什么情况下索引反而有害？

## Redis

14. Redis 为什么适合做限流？
15. ZSET 为什么适合滑动窗口？
16. Lua 为什么必要？
17. Redis 原子性怎么保证？

## JWT

18. Access Token 和 Refresh Token 为什么分开？
19. JWT 为什么需要 JTI？
20. Logout 如何让 JWT 失效？

## RBAC

21. RBAC 怎么设计？
22. Authentication 和 Authorization 有什么区别？
23. 如何防止越权？

## Celery

24. 为什么需要 Celery？
25. Celery 和 FastAPI BackgroundTasks 有什么区别？
26. Celery 任务失败怎么办？
27. 如何保证任务幂等？

## 数据库

28. 为什么需要事务？
29. 什么情况下会出现脏数据？
30. 如何处理并发更新？

## 工程化

31. Docker 为什么使用？
32. Nginx 在项目中做什么？
33. Gunicorn 和 Uvicorn 的关系？
34. GitHub Actions 做什么？
35. 如何设计 CI？

---

# 57. 项目最终验收标准

## 功能

- [ ] 用户注册
- [ ] 用户登录
- [ ] Access Token
- [ ] Refresh Token
- [ ] Logout
- [ ] RBAC
- [ ] Team
- [ ] Team Member
- [ ] Project
- [ ] Task
- [ ] 多人任务分配
- [ ] Task State Machine
- [ ] Comment
- [ ] Attachment
- [ ] Operation Log
- [ ] Notification
- [ ] Redis Rate Limit
- [ ] Celery

## 数据库

- [ ] PostgreSQL
- [ ] Alembic
- [ ] Foreign Key
- [ ] Unique Constraint
- [ ] CHECK Constraint
- [ ] GIN
- [ ] JSONB
- [ ] pg_trgm
- [ ] 合理索引

## 工程化

- [ ] Docker
- [ ] Docker Compose
- [ ] Nginx
- [ ] Gunicorn
- [ ] Uvicorn
- [ ] pytest
- [ ] GitHub Actions
- [ ] README
- [ ] .env.example

## 质量

- [ ] Router / Service / CRUD / Model 分层
- [ ] 核心 Service 有测试
- [ ] API 有测试
- [ ] 核心业务异常有测试
- [ ] 无明文密码
- [ ] 无真实 Secret
- [ ] 无明显 N+1
- [ ] 无明显越权漏洞
- [ ] 无伪造功能

---

# 58. Cursor 开发总 Prompt

以后在 Cursor 中开发本项目时，默认遵循以下规则：

```text
你现在是本项目的资深 Python 后端开发导师。

项目是一个生产级团队任务协作平台后端，技术栈：
Python 3.11/3.12 + FastAPI + SQLAlchemy 2.0 + PostgreSQL 16 + Redis 7 + Celery + JWT + Alembic + Docker + pytest。

项目严格采用：

Router -> Service -> CRUD -> Model

分层架构。

你的任务不是一次性生成整个项目，而是陪我一步一步完成项目。

必须遵守：

1. 一次只完成一个明确功能。
2. 在修改代码之前，先解释设计思路。
3. 如果涉及数据库，先说明 Model、字段、关系、约束、索引。
4. 涉及数据库结构变化时必须使用 Alembic。
5. 核心业务逻辑必须放在 Service。
6. 数据库操作放在 CRUD。
7. Router 不允许承载复杂业务逻辑。
8. 不允许为了“高级”而增加没有实际用途的技术。
9. 不允许虚构接口、数据库字段、ID 或运行结果。
10. 不允许在没有运行测试的情况下声称功能完成。
11. 每完成一个功能，都必须给出验证方法。
12. 优先使用已有代码，不要无理由重构项目。
13. 如果发现已有代码存在问题，先解释问题，再提出最小修改方案。
14. 如果存在多种实现方式，先说明优缺点，再选择适合当前项目的一种。
15. 所有代码必须真实可运行。
16. 所有核心功能必须配套 pytest。
17. 代码必须考虑异常处理、权限、事务和数据一致性。
18. 涉及 Redis 时必须解释数据结构、Key 设计、TTL 和并发问题。
19. 涉及 Celery 时必须解释任务失败、重试和幂等问题。
20. 涉及数据库查询时注意 N+1、索引和分页。
21. 不允许泄露密码、Token、Secret 等敏感信息。
22. 不允许直接修改生产数据库结构。
23. 不允许把复杂 SQL 直接塞进 Router。
24. 不允许一次创建大量无法验证的代码。

开发流程固定为：

需求确认
↓
设计
↓
Model
↓
Alembic
↓
Schema
↓
CRUD
↓
Service
↓
Router
↓
Test
↓
运行测试
↓
修复问题
↓
Git Commit
↓
进入下一个功能

每一步完成后先停下来等待我的“下一步”。

当我说“下一步”时，只执行当前阶段的下一个步骤，不要跳过中间阶段。

如果代码出现报错：
1. 先分析报错原因。
2. 指出具体文件和位置。
3. 解释为什么出现。
4. 给出最小修改方案。
5. 修改后给出验证命令。
6. 不要因为一个局部问题重写整个项目。

项目最终目标是：
构建一个真实可运行、可测试、可 Docker 部署、可通过 GitHub Actions CI、能够用于 Python 后端面试讲解的生产级团队任务协作平台。
```

---

# 59. 第一阶段启动任务

Cursor 第一次开发只执行：

```text
Phase 1：项目骨架
```

具体顺序：

```text
1. 创建 Git 仓库
2. 创建 Python 虚拟环境
3. 创建项目目录
4. 安装基础依赖
5. 创建 FastAPI 应用
6. 创建 config.py
7. 创建 PostgreSQL Docker 服务
8. 创建 Redis Docker 服务
9. 创建 SQLAlchemy session
10. 创建 Alembic
11. 创建 /health
12. 启动 Docker Compose
13. 验证 FastAPI
14. 验证 PostgreSQL
15. 验证 Redis
16. 执行第一次 Git Commit
```

第一阶段完成后，**不要继续开发用户、认证、RBAC 或任务模块**。

必须等 Phase 1 验证通过后，再进入：

```text
Phase 2：用户系统
```

---

# 60. 项目开发终点

最终形成：

```text
一个真实可运行的生产级 FastAPI 后端项目
+
完整 PostgreSQL 数据模型
+
Redis 限流
+
Celery 异步任务
+
JWT 双 Token
+
RBAC
+
任务状态机
+
操作审计
+
通知
+
附件
+
pytest
+
Docker
+
Nginx
+
GitHub Actions
+
完整 README
+
可用于面试的技术难点总结
```

项目核心不是“代码量大”，而是：

```text
业务完整
架构清晰
数据可靠
安全合理
性能可解释
测试真实
部署完整
```

---

# 61. 企业化需求（Phase 18 起）

> 本节是 TASK-088 起的新增需求登记。原始规格（§1~§60）的交付物定义是「可用于面试讲解与
> GitHub 展示的完整项目」；企业交付另有要求。经项目所有者确认，本项目按**企业可直接使用**
> 的标准继续演进，下列需求因此成为规格的一部分。**本节未列出的企业能力仍不在范围内。**
>
> 任务定义与实施顺序见 `docs/TASKS.md`（Phase 18~23 / TASK-088~127）；
> 缺口证据、实测快照与排序理由见 `docs/ENTERPRISE_READINESS.md`；
> 前端相关条目另见 `docs/FRONTEND_PROJECT_SPEC.md`。

## 61.1 已确认的四个边界

1. **目标形态：多租户 SaaS。** 一套部署服务多个租户，租户之间数据、权限、配额严格隔离。
   这是对 §5「核心业务模型」与 §6「RBAC 权限系统」的**结构性修订**——原规格没有租户层。
2. **交付底座：Docker Compose 与 Kubernetes 都要。** 私有化交付走 Compose，云上走 K8s；
   健康检查必须同时满足两种编排的探针语义。
3. **身份档位：本地账号加固 + MFA（TOTP）+ 企业目录（OIDC/LDAP）。** 三者都要；
   MFA 与 SSO 均**可开关**（默认关，不把小规模部署搞复杂）。
4. **不做「最小可交付版」**：按下文全部条目推进，按 Phase 分批实施与验收。

## 61.2 生产可靠性（修订 §32，落实 §1「指标」）

- **§32 修订**：健康检查扩展为 `GET /health`（向后兼容的聚合视图，行为与响应结构不变）、
  `GET /health/live`（存活，仅进程）、`GET /health/ready`（就绪，依赖不可用返回 503）、
  `GET /health/db`、`GET /health/redis`（单依赖明细）。原 §32 只列三个端点且未区分存活/就绪。
- **§1 落实**：交付清单中的「指标」实现为 `GET /metrics`，输出 Prometheus 文本格式
  （由成熟客户端库产出，**不自造格式**），默认不经反向代理对外暴露。
- **§23 / §24 补充**：Celery 周期调度必须真实启用（Beat 服务 + `beat_schedule`）；
  日志归档与附件清理任务在生产**必须被自动触发**，且归档数据必须有终态保留策略
  （到期删除），不得无限期保留。
- **§51 补充**：`APP_ENV=production` 时执行启动自检并**拒绝启动**——默认密钥、
  默认数据库口令、`DEBUG=true`、开启了信任代理头但信任网段为空，均必须点名具体配置项。
- **§50 补充**：未捕获异常必须走 §26 的统一响应信封，进入结构化日志并携带 `request_id`。

## 61.3 多租户（新增）

- 引入租户实体与生命周期（启用 / 停用 / 删除），全站资源归属到租户。
- **§5 修订**：原「username、email 唯一」改为**租户内唯一**——唯一约束一律带租户维度，
  不同租户可以存在同名用户与同名团队。
- 数据访问必须有**租户作用域强制**：应用层统一作用域 + PostgreSQL 行级安全（RLS）兜底；
  跨租户访问一律返回与「不存在」不可区分的响应（延续 §49 的防枚举口径）。
- 认证令牌必须携带租户身份；**不允许一个令牌横跨两个租户**，切换租户须换取新令牌。
- 租户级配额（成员数、存储量、限流档位）必须被**执行**，而不是只存数字。
- **§6 修订**：RBAC 改为**租户内**角色与权限；平台管理员与租户管理员严格区分，
  平台管理员不自动获得租户内权限。

## 61.4 身份与安全（修订 §19 / §20 / §21 / §48，新增 MFA 与 SSO）

- **§20 修订**：密码必须有强度校验（最小长度、字符类、常见弱口令、不得包含用户名或邮箱），
  必须提供改密与**自助重置**路径；改密后该用户既有会话全部失效。
- **登录防护（新增）**：失败计数（账号与 IP 双维度）、阈值锁定与自动解锁、
  登录端点独立限流档位；错误文案不得泄露账号是否存在（延续 §48 的防枚举要求）。
- **§19 修订**：令牌增加 `iss` / `aud` 声明并强制校验；支持多密钥（`kid`）与轮换验证窗口。
- **§21 落地**：`jwt_blacklist_key()` 从「只定义 Key 约定」升级为**接入校验链路**——
  登出 / 禁用账号 / 改密 / 会话下线后，Access Token 必须**立即**失效，而不是等 TTL 到期。
  同时必须给出 Redis 不可用时的行为，并把这一取舍显式记录。
- **会话管理（新增）**：提供会话列表、逐会话下线、全部下线；检测到 Refresh Token 重用时
  吊销该用户**全部**会话并记录安全事件。
- **MFA 与企业目录（新增）**：TOTP 双因子（含一次性恢复码）与 OIDC / LDAP 单点登录，
  均可开关，且按租户配置。
- **§31 补充**：落地 `Content-Security-Policy`（先 Report-Only 观察再强制）、
  `Permissions-Policy` / `COOP` / `COEP`；明确 TLS 的终止位置与证书续期责任方。

## 61.5 合规与数据治理（新增）

- 用户可导出自己的数据、可注销账号（软删 + 数据处置策略）。
- 每类数据的保留期有明文声明**且由代码执行**。
- **§15 补充**：审计日志增加来源信息（IP、User-Agent）与筛选能力（类型 / 时间范围 /
  操作人），并提供归档数据的查询入口；部署文档须说明应用数据库账号不持有归档表的
  `UPDATE` / `DELETE` 权限。
- 合规档位按「个人信息保护法 + GDPR 口径」执行；等级保护与行业认证**不作为代码承诺**。

## 61.6 产品能力（补齐 §9~§18 与 §47 的欠项）

- **邮件（新增）**：SMTP 抽象 + 可插后端 + 模板，全部经 Celery 队列发送，不阻塞请求路径；
  用于成员邀请、密码重置、通知摘要与到期提醒。
- **§18 修订**：通知必须携带资源定位信息（资源类型 + 资源 id 或链接），前端可跳转到
  对应资源；补评论通知、`@` 提及与到期提醒。
- **§47 修订**：列表响应必须包含总数（`total`），分页口径全站统一；若后续引入游标分页，
  必须作为一次显式决策记录，不得顺手替换。
- **个人视图（新增）**：提供跨项目的「我的任务」与工作台聚合。
- **统计（新增）**：项目与团队提供真实的任务统计与进度口径（分母定义必须写明）。
- **实时通知（新增）**：以 SSE 提供推送（优先于 WebSocket）；多副本下经 Redis 广播。
- **批量操作与导入导出（新增）**：批量指派 / 流转 / 删除（**逐条**校验权限与状态机）、
  成员 CSV 导入、数据导出（受配额约束）。

## 61.7 工程与交付（修订 §29 / §30 / §37 / §45）

- **备份与恢复（新增）**：数据库与附件的定时备份 + 恢复脚本 + **恢复演练**，并声明
  RPO / RTO；必须明确多租户下的恢复粒度与能力边界。
- **日志聚合与告警（新增）**：日志可聚合检索（按 `request_id`），指标告警规则与
  on-call runbook 一一对应。
- **§37 补充**：CI 增加依赖漏洞扫描与镜像扫描、SBOM 产出、依赖更新自动化；
  豁免必须显式且可追溯，不允许静默跳过。
- **发布流程（新增）**：staging 环境、迁移上线门禁、发布清单与回滚 runbook。
- **§17 附件存储修订**：必须实现对象存储后端（S3 / MinIO）作为本地后端的对等实现
  （多副本部署的前置条件），并增加文件内容嗅探——不信任扩展名与客户端声明的 MIME。
- **交付形态**：Compose 补资源上限与容量估算；提供 Kubernetes 清单（存活 / 就绪探针按
  §61.2 的语义；**Celery Beat 必须单副本**）。
- **§45 落实**：建立压测与慢查询基线，连接池与容量参数须有数据依据。
- **前端测试（新增）**：前端规格要求的组件测试与端到端测试（Playwright）落地；
  前端错误上报并携带 `request_id` 以关联后端日志。
- **前端国际化与可访问性（新增）**：i18n（中 / 英）、a11y 基本要求、浏览器支持矩阵。

## 61.8 明确不做

延续 §6 与项目规则，以下**不引入**：第二个 Redis / Kafka / RabbitMQ；Elasticsearch；
自研 SSO 或自建 CA；微服务拆分；为覆盖率数字写空测试。
